{
  "name": "flatten-subcollection-snapshots",
  "version": "1.1.0",
  "description": "Interpolates nested subcollections of a Firebase collection into a single object, and listens for changes along all levels.",
  "license": "MIT",
  "main": "index.js",
  "types": "index.d.ts",
  "dependencies": {
    "@types/firebase": "^3.2.1",
    "firebase": "^8.10.1"
  },
  "scripts": {
    "prepublish": "tsc",
    "publish": "npm publish"
  }
}
